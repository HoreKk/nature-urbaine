{
	"name": "@nature-urbaine/emails",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"dev": "email dev --dir src/previews --port 4000",
		"export": "email export --dir src/previews",
		"format": "oxfmt",
		"format:check": "oxfmt --check",
		"lint": "oxlint",
		"lint:fix": "oxlint --fix",
		"check": "oxfmt --check && oxlint",
		"check-types": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"react": "^19.2.0",
		"react-dom": "^19.2.0",
		"react-email": "^6.0.5"
	},
	"devDependencies": {
		"@react-email/ui": "^6.0.5",
		"@types/node": "^22.10.2",
		"@types/react": "^19.2.0",
		"typescript": "^5.7.2",
		"vitest": "^3.0.5"
	}
}
